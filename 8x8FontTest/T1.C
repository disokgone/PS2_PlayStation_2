#include <stdio.h>

void	test(int a, int b, int c, int d, int e, int f, int g, int h, int i, int j)
{
char	*buf;
int	app;

sprintf(buf, "app = %d", app);
a = e + f;
b = g + h;
c = i + j;
}
